# Public workflows

Only GitHub Actions workflow files intended for the public source repository belong in this directory. Public CI can add its workflow YAML here without copying any private repository workflow metadata.
